<h2>New submission</h2>

<%= render "form.html", changeset: @changeset,
                        action: user_submission_path(@conn, :create, @user) %>

<%= link "Back", to: user_submission_path(@conn, :index, @user) %>
